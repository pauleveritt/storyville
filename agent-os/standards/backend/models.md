# Database Models

- Singular model names, plural table names
- Include created/updated timestamps
- Use DB constraints (NOT NULL, UNIQUE, foreign keys)
- Index foreign keys and frequently queried fields
- Validate at both model and DB levels
- Define clear relationships with cascade behaviors
