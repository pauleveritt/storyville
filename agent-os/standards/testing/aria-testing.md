# Aria Testing

- Test component rendering using the aria-testing package
- A view should render to an `tdom.Element` or occassionally a `tdom.Fragment`
- Use the queries in aria-testing to make assertions about what should be in the result