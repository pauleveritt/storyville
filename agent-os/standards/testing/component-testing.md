# Component Testing

## File names

- Put the test file alongside the component, in the same directory
- Suffix the filename with `_test.py`
- Thus a component `MyHeading` would have a component file of `my_heading.py` and a test file of `my_heading_test.py`
  