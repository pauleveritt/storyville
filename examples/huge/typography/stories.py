"""The Typography section for the huge example."""

from storyville import Section


def this_section() -> Section:
    """Typography section with typography components."""
    return Section(
        title="Typography",
        description="Typography components",
    )
